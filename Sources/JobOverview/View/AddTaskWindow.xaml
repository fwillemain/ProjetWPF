<Window x:Class="JobOverview.View.AddTaskWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JobOverview.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Name="UCAddTask">
    <Window.Resources>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
        </Style>
        <DataTemplate x:Key="dtTaskAnnex">
            
        </DataTemplate>
        <DataTemplate x:Key="dtTaskProd">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label Content="Logiciel :"/>
                    <ComboBox Name="cbSoftwareSelection"/>
                    <Label Content="Version :"/>
                    <ComboBox Name="cbVersionSelection"/>
                    <Label Content="Module :"/>
                    <ComboBox Name="cbModuleSelection"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label Content="Label :"/>
                    <TextBox Name="tbxLabel" MinWidth="100"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label Content="Temps prévu"/>
                    <TextBox Name="tbxPredictedTime" MinWidth="30"/>
                    <Label Content="Temps restant estimer"/>
                    <TextBox Name="tbEstimatedRemainingTime" Text="{Binding ElementName=tbxPredictedTime, Path=Text}" MinWidth="30"/>
                </StackPanel>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Content="Activité :"/>
            <ComboBox Name="cbTypeTask" ItemsSource="{Binding CurrentEmployee.Job.ListActivity}">
            </ComboBox>
        </StackPanel>
        <ContentControl ContentTemplate="{StaticResource dtTaskProd}"/>
        <StackPanel Orientation="Horizontal">
            
        </StackPanel>
    </StackPanel>
</Window>
